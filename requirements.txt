# ── Core scraping stack (existing) ───────────────────────────────────────────
beautifulsoup4>=4.12.2
lxml>=4.9.3
pika>=1.3.0
PyYAML>=6.0
requests>=2.31.0
streamlit>=1.28.0
pytest>=8.0.0
python-dotenv>=1.0.0

# ── Async HTTP (new — async retriever/discovery workers) ─────────────────────
aiohttp>=3.9.0
aio-pika>=9.4.0           # async RabbitMQ client (wraps aiohttp + pika)

# ── FastAPI control plane ─────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0

# ── Redis (URL dedup Bloom filter + rate limiter + change detection) ──────────
redis[asyncio]>=5.0.0     # includes redis.asyncio

# ── Observability ─────────────────────────────────────────────────────────────
prometheus-client>=0.20.0
structlog>=24.1.0

# ── Storage ───────────────────────────────────────────────────────────────────
boto3>=1.34.0             # S3 raw HTML storage (optional — only needed if SARA_S3_BUCKET set)

# ── Data processing ───────────────────────────────────────────────────────────
pandas>=2.0.0             # dashboard data tables + analytics

# ── Dashboard extras ──────────────────────────────────────────────────────────
plotly>=5.20.0            # charts in Streamlit analytics page
httpx>=0.27.0             # dashboard → API calls

# ── Scheduler ─────────────────────────────────────────────────────────────────
APScheduler>=3.10.0       # per-site crawl scheduling

# ── File locking (cross-process crawl_status.json safety) ────────────────────
# fcntl is used directly on Linux (stdlib); no extra package needed.

# ── Elasticsearch ─────────────────────────────────────────────────────────────
elasticsearch>=8.0.0      # ES client + bulk helpers (optional — only needed if ELASTICSEARCH_URL set)

# ── Playwright (optional — headless browser fallback for JS sites) ────────────
# playwright>=1.44.0      # uncomment + run: playwright install chromium
