---
domain: flipkart.com
fields:
  crawl_timestamp:
    desc_of_xpath: "Crawl timestamp"
    standard_nodeset_range: first
  uniq_id:
    desc_of_xpath: "Unique id"
    standard_nodeset_range: first
  page_url:
    desc_of_xpath: "Page URL"
    standard_nodeset_range: first
  product_name:
    desc_of_xpath: "Product title (from JSON-LD or h1)"
    standard_nodeset_range: first
  brand:
    desc_of_xpath: "Brand name (from JSON-LD)"
    standard_nodeset_range: first
  selling_price:
    desc_of_xpath: "Current selling price in INR (from JSON-LD offers.price)"
    standard_nodeset_range: first
  list_price:
    desc_of_xpath: "Original MRP in INR (from HTML price block)"
    standard_nodeset_range: first
  discount_percentage:
    desc_of_xpath: "Discount percentage"
    standard_nodeset_range: first
  rating:
    desc_of_xpath: "Average star rating (from JSON-LD aggregateRating)"
    standard_nodeset_range: first
  num_reviews:
    desc_of_xpath: "Number of ratings (from JSON-LD aggregateRating)"
    standard_nodeset_range: first
  sku:
    desc_of_xpath: "Product SKU (from JSON-LD)"
    standard_nodeset_range: first
  color:
    desc_of_xpath: "Product color (from JSON-LD)"
    standard_nodeset_range: first
